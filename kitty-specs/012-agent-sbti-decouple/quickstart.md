# Quickstart: 012 Agent SBTI Decouple

1. 打开页面，先记住当前 `Task 6` 的 Agent SBTI
2. 在 Hero 输入 `CTRL`
3. 进入 Journey，确认 `Task 6` 的 Agent SBTI 不因输入变成 `CTRL`
4. 再把 Hero 输入改成 `SOLO`
5. 确认 `Task 6` 仍然保持同一个 Agent SBTI
6. 跑 `npm run test` 与 `npm run build`
