# Contracts

本 mission 不引入新的后端 API contract。  
当前 contract 关注点仅限前端交互约束：

- desktop/mobile 分享入口可见性 contract
- mobile copy + countdown + app handoff contract
- completed task expand/collapse contract
- active stage loading state contract

后续如果需要把分享 handoff 或运行日志上报到服务端，再补具体 API schema。
