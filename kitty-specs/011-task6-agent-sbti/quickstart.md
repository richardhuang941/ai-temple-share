# Quickstart: 011 Task 6 Agent SBTI

1. 打开页面
2. 在 Hero 输入 `CTRL`
3. 启动模拟并推进到 `Task 6`
4. 确认 `Task 6` 显示 `Agent SBTI`，且结果与 `CTRL` 对应
5. 再输入一个未知 code，如 `ABCD`
6. 重新看 `Task 6`，确认显示 fallback，但流程不报错
7. 检查页面中不再出现 `LBTI`
