# Quickstart: 009-agent-temple-brand-neutralization

```bash
npm run test
npm run build
```

手工检查：

1. Header 显示 `Agent Temple`
2. 首页与分享文案显示 `Agent Temple AI`
3. 页面 title / meta / noscript 统一为 `Agent Temple`
4. 真实 repo/path 与 domain 未被误改
